If the 'test' job fails, the 'build-docker' job is automatically skipped (it will not run). 

This happens because the build job is dependent on the test job (using "needs: test"). This is a critical concept in CI/CD because it acts as a quality gate; it prevents the pipeline from wasting resources building a Docker image for broken code, and more importantly, it prevents a broken application from ever reaching the deployment stage.
